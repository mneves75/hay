{
  "name": "hay",
  "private": true,
  "packageManager": "bun@1.4.0",
  "description": "Dev-only: type definitions and tsc for the typecheck gate. The tools themselves are runtime-dependency-free and run under bun, which strips types without checking them — which is how a dead property access (`a.results` on an object that has `scanned`) shipped in four versions of the harness.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint:ast-grep": "ast-grep scan --error --report-style=short .",
    "test:ast-grep": "ast-grep test --skip-snapshot-tests"
  },
  "devDependencies": {
    "@ast-grep/cli": "0.45.1",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
