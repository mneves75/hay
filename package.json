{
  "name": "hay",
  "private": true,
  "packageManager": "bun@1.4.0",
  "description": "TypeScript measurement tools for hay. They run under Bun; the SWE archive harness uses the maintained tar parser, while type definitions and tsc enforce the typecheck gate.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint:ast-grep": "ast-grep scan --error --report-style=short .",
    "test:ast-grep": "ast-grep test --skip-snapshot-tests"
  },
  "devDependencies": {
    "@ast-grep/cli": "0.45.1",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "tar": "7.5.22"
  }
}
