# 2026-08-26

## 0.1.4 integrity release

- Completed the repository-native issue 12 contract without changing ranking signals, weights, or
  the failed pre-registered behavioural gate.
- Corrected exact-ref installation, deterministic install roots, private corpus boundaries,
  archive extraction limits, subprocess failure semantics, stable-order inference, timeout
  censoring, benchmark provenance, generated-report validation, accessibility, and film evidence.
- Final P3 autoreview findings were reproduced before fixing: Cargo destination mismatch,
  unescaped film release-ref validation, incomplete explainer provenance, and unfocusable table
  scrollers. Focused positive controls cover each correction.
- Local gates passed: 50 Rust tests, clippy, format, release build, 17/17 differential parity,
  all nine TypeScript selftests, frozen installs, typechecks, ast-grep positive controls,
  shellcheck, generated-artifact drift, Bun/pnpm/RustSec audits, and a clean current-tree secret
  scan. Browser E2E reported zero axe violations, errors, console output, or overflow.

## Staging

- Pushed reviewed main commit `cce42179dae8b2a6f25e9dd22f169712fb9e8bb1`; CI run
  `33031310741` passed all seven jobs.
- Annotated `v0.1.4-beta1` points to that commit. Release run `33031510806` passed five
  platform builds and created an unpublished draft with ten checksummed assets and SLSA
  attestations.
- Downloaded the arm64 macOS archive, verified its checksum and GitHub/Sigstore provenance, then
  ran it natively: `hay 0.1.4` and a search smoke test passed.

## Pending

- Stable tag `v0.1.4` and the production Pages update require the fresh production confirmation.
- Publishing the draft GitHub Release is a separate explicit action.
- The independent verifier found no P0/P1 code, methodology, installer, or security blocker, but
  could not complete a final pixel inspection because the local image viewer hung. Browser DOM,
  axe, overflow, OCR, and media metadata checks passed.
