# 2026-08-23 — 0.1.1: the release CI wrote

The 0.1.0 release shipped with CI that had never executed (the repo was private). Its first
public runs failed, and every item in 0.1.1 came from reading those failures rather than
re-running them:

- **The MSRV claim was false.** The committed lockfile pins `globset 0.4.20` / `ignore 0.4.33`,
  which declare `rust-version = "1.88"` — upstream ripgrep now sets MSRV metadata on its crates
  and tracks recent stable. Our "MSRV 1.85" compiled locally only because the local toolchain is
  1.98; the dedicated MSRV job was the only thing positioned to notice, and it did, on its first
  run. Bumped to 1.88 in the manifest, the CI job pin (comment now says to keep them in sync),
  the installer gate, AGENTS.md, MANUAL, and the site chip.
- **A generated file was stale.** `benchmark-feynman.html` in the tree lacked the live-companions
  paragraph its generator emits — the staleness gate (`git diff --exit-code` after regeneration)
  caught it on first execution. Regenerated.
- **The installer had three defects nobody had hit.** The advertised one-liner piped to `sh`
  while the script uses `pipefail` (dash rejects it); the "installing to" message appended `/bin`
  to a path already ending in `/bin`; and — found by codex autoreview at P3, verified real —
  the install dir was derived from `dirname $(command -v cargo)`, which is wrong for Homebrew or
  distro cargo. Now derived by cargo's own resolution order (`CARGO_INSTALL_ROOT` →
  `CARGO_HOME` → `~/.cargo`), and confirmed end-to-end by running the live installer from main:
  installed 0.1.1 into `~/.cargo/bin`, smoke test green.
- One more CI round: the runner's shellcheck fails on info-severity SC2015 that the local one
  passed — the smoke-test `&& … || true` became a plain `if`. Lesson: adding a file to a linter
  gate means the CI linter's version decides, not yours.

Ship chain: commit → push → CI green (**first fully green public run**: 32662935816, all seven
jobs) → `v0.1.1-beta1` pre-release (tarball downloaded back and smoke-tested, installer verified
live) → `v0.1.1` Release with sha256 in the notes → gh-pages updated (index chip + version,
MANUAL/benchmark refreshed to release editions, Feynman links kept on the site's `.html`
convention) → all four live URLs verified 200 with the new content.

Blocker #5 (cross-platform CI unverified) is closed. The durable lesson is the one this repo
already teaches in another costume: a gate that has never executed is a claim, not a gate —
both first-run failures were real defects sitting under a green-looking local workflow.

# 2026-08-23, later — cycle 2 opens: the docs track measures, and blocks its own next step

Ticket 11 pre-registered the cycle-2 plan (docs-track dev set → shape-conditional prior gated on
its counts → deep-rank bucket → weight fitting → external validity → token-outcome measure).
P1 and P7 shipped the same day; the interesting outcome is what P1 *refused to license*.

**P1 — docs track** (`benchmark.ts --docs-track`, implemented by a codex sol-xhigh lane against
the pre-registered derivation rule, reviewed line-by-line here, all gates re-run locally):
heading-anchored public ground truth, same rank unit/cap/argv/stats as the code track, own
evidence file, fail-closed validation, byte-identical reports when absent. First published run
(n=30/corpus): **hay loses on doc-answerable queries by −0.482 MRR [−0.626, −0.337] (ripgrep
corpus) and −0.318 [−0.455, −0.191] (alamofire), both tests p ≈ 0.0001**; delta 0 on this repo
as corpus. The prose bucket of issue 10 now has a public number.

**Autoreview (codex sol, P3, 5 findings) changed the published finding itself.** The first cut
reported "shape splits ~all plainWord" — built on a misclassification: PascalCase had no bucket,
so `AuthenticationInterceptor` counted as a plain word. Corrected (own DOC_SHAPES bucket,
CommonMark indented-heading fix, evidence re-run): **the dominant doc-answerable shape is
PascalCase type names documented in markdown** — 27/30 on alamofire (Δ −0.365), Δ −0.783 on
ripgrep's pascal bucket (n=8). Numbers moved: ripgrep −0.482 → −0.440, alamofire −0.318 →
−0.340. The P2 gate decision *survives with better reasons*: PascalCase is exactly the code
track's type-query shape, so a shape-keyed damp would fire where the definition signal earns
its living. Next conditioning candidate is corpus-side (share of a token's matches landing in
prose at ranking time); needs its own pre-registration. Also from review: the docs validator now
recomputes every deterministic aggregate from per-query rows (finite-but-false refuses);
release.yml refuses to touch a published (non-draft) release. One finding rejected on facts:
"aarch64 cannot execute on Intel macos-latest" — macos-latest is arm64 and GH's images ship
Rosetta 2 for the x86_64 check; documented in the workflow instead.

**P7 — release engineering**: `release.yml` (5 targets, sha256s, tag-matches-binary guard,
draft release only — publishing stays human); differential test now runs on Windows under Git
Bash with choco ripgrep — the parity claim had never executed there. 0.1.2, no ranking change.

# 2026-08-23, night — P2′: implemented, measured, refused

The corpus-side doc prior (damp the prose penalty when the query's own match stream is
prose-dominated — a post-retrieval QPP-style predictor) went the full pre-registered route: registered
in ticket 11 BEFORE code, implemented by a codex lane (prose_damp, post-walk share over the
complete per_file/total stream, `--no-docprior`, env knobs failing loudly, 37+13 tests,
differential 0), grid run as registered. **hay's numbers did not move at any grid point** on the
selection corpora — the conditioning feature is false in the world: doc-answerable queries have
prose shares of 0.01–0.18, indistinguishable from code queries, because the doc answer is a
needle in a code haystack (which is exactly why hay buries it — same fact, two costumes). Stop
rule fired; implementation stashed, not shipped; ticket records the probe counts. The
prospective discipline now has both outcomes in one day: P1's counts blocked a signal before
implementation, P2′'s measurement refused one after. Incidental: rg's benchmark MRR wobbles
across identical runs (parallel output order) — alamofire docs-track rg spanned 0.275–0.402
over four runs; noted in the ticket as the noise floor under every rg point estimate.

Lane ops note: `codex-lane start` + `-c model_reasoning_effort=xhigh` delivered a clean,
spec-conforming diff in one round (~15 min), including a fixture correction it explained.
The one spec ambiguity (hyphenated tokens only via leading-hyphen) was resolved as registered
and flagged rather than improvised. Review found nothing to change — first time this repo's
delegation produced a zero-amendment round.

Advisor-lens sweep (bounded): nothing new in the domain demands a repo change. The 2026
literature that surfaced (Sourcegraph's BM25-over-embeddings retreat, Aider's PageRank ranking,
the "grep is having a moment" essays, CoREB's finding that short keyword queries collapse
semantic models to near-zero nDCG@10) all lands on the side hay already occupies — lexical,
stateless, ranked, with `rg` kept for exhaustive sweeps. CoREB would be a citable support for
DESIGN-hay if a citations pass ever happens; noted as optional, not done.
